<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Product">
    <insert id="product_write" parameterType="lotte.com.toy.dto.ProdcutDto">
        insert into products(product_name, product_info, product_img, product_cost, product_stock, product_sell_count,
                             category_id, seller_id)
        values (${product_name},${product_info},${product_img},${product_cost},${product_stock},${product_sell_count},${category_id},${seller_id});

    </insert>
    <select id="findByProductId" parameterType="Integer" resultType="lotte.com.toy.dto.ProdcutDto">
        select product_id,product_name,product_info,product_img,product_cost,product_stock,product_sell_count,category_id,seller_id
        from products
        where product_id = #{product_id}
    </select>
</mapper>